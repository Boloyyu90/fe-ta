import { RegisterForm } from '@/features/auth/components/RegisterForm';

export const metadata = {
    title: 'Daftar - Tryout System',
    description: 'Buat akun baru',
};

export default function RegisterPage() {
    return <RegisterForm />;
}