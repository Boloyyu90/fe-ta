import { LoginForm } from '@/features/auth/components/LoginForm';

export const metadata = {
    title: 'Masuk - Tryout System',
    description: 'Masuk ke akun Anda',
};

export default function LoginPage() {
    return <LoginForm />;
}